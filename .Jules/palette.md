## 2024-04-17 - Add aria-label to icon-only navigation buttons
**Learning:** Icon-only custom navigation buttons (e.g., `.nav-btn`, `.skill-nav-btn`, `.bk-nav-btn`, `.ctrl-btn`) in this repository's customized UI lack screen reader context by default.
**Action:** Always add descriptive `aria-label` attributes to these elements to ensure accessibility.
