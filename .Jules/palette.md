## 2024-05-18 - ARIA labels for icon-only buttons
**Learning:** Icon-only custom navigation buttons in customized UIs lack screen reader context by default, making them inaccessible.
**Action:** Always add descriptive `aria-label` attributes to these elements (`.nav-btn`, `.skill-nav-btn`, `.bk-nav-btn`, `.ctrl-btn`, etc.) to ensure accessibility.
