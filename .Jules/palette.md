## 2024-05-24 - Keyboard Support for Custom Swiper
**Learning:** Custom interactive widgets (like swipe carousels) require manual semantic roles, `tabindex`, and keyboard event handlers with `preventDefault()` to prevent scrolling.
**Action:** Always verify keyboard focus and Arrow key support for any custom widget relying on mouse/touch events.
